import type { GatsbyConfig } from "gatsby"

const config: GatsbyConfig = {
  siteMetadata: {
    title: `inolopesm.dev`,
    siteUrl: `https://inolopesm.dev`,
  },
  plugins: [`gatsby-plugin-react-helmet`],
}

export default config
